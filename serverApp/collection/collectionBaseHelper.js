const utils = require("../utilities/utils");

const buildMongoDbSetObject = (currentDocument) => {
  let setObject;
  for (const currentField in currentDocument) {
    // recursive olacak gibi...
  }
};

module.exports = {
  buildMongoDbSetObject,
};
